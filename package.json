{
  "name": "grocery-intelligence-pdf-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Client-side PDF grocery circular comparison studio",
  "scripts": {
    "check": "node --check app.js && node --check studio.js",
    "test": "node --test tests/*.test.js"
  }
}